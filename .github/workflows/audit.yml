name: Cargo Audit

on:
  push:
    branches:
      - main
  schedule:
    # Run weekly on Monday at 6 AM UTC
    - cron: '0 6 * * 1'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  audit:
    name: Audit Dependencies
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install cargo-audit
        run: cargo install cargo-audit

      - name: Run cargo audit
        run: cargo audit
        continue-on-error: true
        id: audit

      - name: Audit Summary
        run: |
          echo "## Cargo Audit Results" >> $GITHUB_STEP_SUMMARY
          cargo audit --json | jq '.vulnerabilities.count // (.vulnerabilities | length)' | xargs -I {} echo "Found {} vulnerabilities" >> $GITHUB_STEP_SUMMARY || echo "Audit completed" >> $GITHUB_STEP_SUMMARY

      - name: Fail if vulnerabilities found
        if: steps.audit.outcome == 'failure'
        run: exit 1
